@page
@model training_health_medical_app.Pages.Coaches.CreateModel

@{
    ViewData["Title"] = "Create";
}

<div class="container mt-5">
    <h1 class="text-center mb-4">Create Coach</h1>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Coach.Name" class="control-label">Name</label>
                    <input asp-for="Coach.Name" class="form-control" />
                    <span asp-validation-for="Coach.Name" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Coach.Gender" class="control-label">Gender</label>
                    <input asp-for="Coach.Gender" class="form-control" />
                    <span asp-validation-for="Coach.Gender" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Coach.Sport" class="control-label">Sport</label>
                    <input asp-for="Coach.Sport" class="form-control" />
                    <span asp-validation-for="Coach.Sport" class="text-danger"></span>
                </div>
                <div class="form-group text-center">
                    <button type="submit" class="btn btn-primary">Create</button>
                </div>
                @if (Model.CoachExists)
                {
                    <div class="alert alert-danger" role="alert">
                        Coach already exists for the current user.
                    </div>
                }
            </form>
        </div>
    </div>

    <div class="text-center mt-3">
        <a asp-page="Index" class="btn btn-secondary">Back to List</a>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
